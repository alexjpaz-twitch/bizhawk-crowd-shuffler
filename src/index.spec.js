const { expect } = require('chai');
const sinon = require('sinon');

const { Application } = require('./');

describe('@wip Application', () => {
  it('construct', () => {
    new Application();
  });
});
